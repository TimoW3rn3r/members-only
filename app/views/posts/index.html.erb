<h1>Posts</h1>

<% @posts.each do |post| %>
  <h3><%= post.title %></h1>
  <% if user_signed_in? %>
    <div>
      - <%= post.user.first_name %> <%= post.user.last_name %>
    </div>
  <% end %>
  <p><%= post.body %></p>
<% end %>

<%= link_to "New Post", new_post_path %>
